<h1>Join a group</h1>
<% @groups.each do |group| %>
  <%= link_to group_path(group) do %>
    <div class="d-flex mb-4">
      <%= cl_image_tag group.photo.key, height: 55, width:55 , crop: :fill, style: "object-fit: cover;", class:'logo'%>
      <%= group.name %>
    </div>
  <% end %>
<% end %>
